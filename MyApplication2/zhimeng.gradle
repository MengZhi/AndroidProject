ext {
    isRelease = false

    url = [
            debug  : "www.debug.com",
            release: "www.release.com"
    ]

    androidId = [
            compileSdkVersion        : 31,
            applicationId            : "com.example.mengzhi.myapplication2",
            minSdkVersion            : 31,
            targetSdkVersion         : 31,
            versionCode              : 1,
            versionName              : "1.0",
            testInstrumentationRunner: "android.support.test.runner.AndroidJUnitRunner"
    ]
}